# ~/.zshenv: executed by the command interpreter for all shells

# Fix gnome-terminal color support
if [[ "$COLORTERM" == "gnome-terminal" ]] ; then
  if [[ "$TERM" == "xterm" ]] ; then
    TERM="xterm-256color"
  elif [[ "$TERM" == "screen" ]] ; then
    TERM="screen-256color"
  fi
fi

# Make sure everyone knows I use vim
EDITOR=`which vim`
VISUAL=`which vim`
# # Try using Spacemacs for a while
# export ALTERNATE_EDITOR=""
# export EDITOR=emacsclient

######
# Everything below here must be exported.
#####

if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi

# Update PATH for the Google Cloud SDK.
source "$HOME/.local/share/google-cloud-sdk/path.zsh.inc"

# Update PATH for fzf.
if [[ ! "$PATH" == *$HOME/.local/share/fzf/bin* ]]; then
  export PATH="$PATH:$HOME/.local/share/fzf/bin"
fi

# Update PATH for depot_tools, as long as I work on Chromium.
PATH="$HOME/.local/share/depot_tools:$PATH"
MANPATH=":$HOME/.local/share/depot_tools/docs"

# Set up GOPATH, assuming that I have a /work drive.
if [ -z "$GOPATH" ]; then
  GOPATH="/work/go"
  PATH="$GOPATH/bin:$PATH"
fi

export GOPATH
export MANPATH
export PATH
